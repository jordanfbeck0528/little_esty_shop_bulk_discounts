<div class="nav-container">
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <p class="navbar-text"><%= @merchant.name %></p>
  </nav>
</div>

<body>
  <div class="edit-discount-info">
    <%= form_with url: merchant_discount_path(@merchant.id, @discount.id), method: :put do |form| %>
      <%= form.label :percentage, "Discount Percentage Threshhold:" %>
      <%= form.text_field :percentage %><br><br>
      <%= form.label :quantity, "Discount Quantity Threshhold:" %>
      <%= form.text_field :quantity %><br><br>
      <%= form.submit "Edit This Discount" %>
    <% end %>
  </div>
</body>
